<html>
<head>
    <title>Book list</title>
    <meta name="description" content="Book list page."/>

    {{> assets.hsb}}

</head>
<body class="components-page">

    <div class="wrapper">
        <div class="main main-raised">
            <div class="section">
                <p>{{message}}</p>
                <!--
                <p>Numer of books:{{booksNumber}}</p>
                {{#each books}}
                    <p>{{this.name}}, {{this.description}}, {{this.bookId}}</p>
                {{/each}}
                -->
                <table class="table">
                    <thead>
                    <tr>
                        <th>Name</th>
                        <!--
                        <th>Summary</th>
                        <th class="text-right">Action</th>
                        -->
                    </tr>
                    </thead>
                    <tbody>
                    {{#each books}}
                    <tr>
                        <td>{{this}}</td>
                        <!--
                        <td>{{this.description}}</td>
                        <td class="text-right">
                            <a href="/books/readers/{{this.bookId}}"><button class="btn">READ</button></a>
                            <a href="/books/info/{{this.bookId}}"><button class="btn">INFO</button></a>
                        </td>
                        -->
                    </tr>
                    {{/each}}
                    </tbody>
                </table>

            </div>

            <!--
            <nav>
                <a href="/books/read" data-info="read book"><button class="btn btn-primary">Read it</button></a>
            </nav>
            -->
        </div>
    </div>

</body>
</html>