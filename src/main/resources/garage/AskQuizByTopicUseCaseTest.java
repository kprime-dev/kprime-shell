package it.nipe.garage.quiz.usecase;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

public class AskQuizByTopicUseCaseTest {

    @Test
    public void test_random_quiz_by_topic() {
        /*
        // Given
        QuizRepository quizRepository = new QuizRepositoryRam();
        Quiz quiz = new Quiz("question","correct-answer");
        quiz.setTopic("geography");
        quizRepository.add(quiz);
        UserRepository userRepository = new UserRepositoryRam();
        String studentName = "bob";
        // Do
        String topic = "geography";
        Optional<Quiz> randomQuiz = AskQuizByTopicUseCase.getRandomQuiz(quizRepository,userRepository,topic,studentName);
        // Then
        assertThat(randomQuiz.get().getQuestion(),is("question"));
        */
    }
}
