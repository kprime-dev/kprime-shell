package it.nipe.garage.domain;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

public class QuizTest {
    @Test
    public void test_answer_counter() {
        /*
        Quiz quiz = new Quiz("question","correct");
        quiz.addAnswer("wrong1");
        quiz.addAnswer("wrong2");
        quiz.addAnswer("wrong3");
        quiz.addAnswer("correct");
        quiz.addAnswer("correct");
        quiz.addAnswer("correct");
        assertThat(quiz.getAnswerNumber(),is(6l));
        */
    }

}
